import React from 'react';
import renderer from 'react-test-renderer';

import UserPage from '../UserPage';

describe('User: UserPage', () => {
  it('Test ResizeDetector', () => {
    expect(true).toBe(false);
  });
});
